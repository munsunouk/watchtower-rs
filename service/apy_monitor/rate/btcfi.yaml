name: BTCFI Boost Rate Check
time_interval: 300
script: |
  notify_rate = 3;
  boost_apy = Boost.BoostApy(Boost);

  when boost_apy < notify_rate then Slack.Send(APY, '*BTCFI Boost APY 알람* 🚀\n <!here>\n> 현재 APY: ' + boost_apy + '\n> APY 확인 필요합니다.') else false;
