name: Bifrost OracleManager USDT Insufficient Check
time_interval: 300
script: |
  bifrostBN = Bifrost.LatestBlock();
  currentTimestamp = Bifrost.LatestTimestamp();

  feeder = Bifrost.CCCP.Feeder.Relayer(bifrostBN);
  decisionInterval = Bifrost.OracleManager.TimeFilter.Decision(bifrostBN, USDT);
  lastDecision = Bifrost.OracleManager.Info.LastTimestamp(bifrostBN, USDT);

  secPastSinceLastDecision = currentTimestamp - lastDecision;
  secPastSinceLastDecision < decisionInterval - 300;
