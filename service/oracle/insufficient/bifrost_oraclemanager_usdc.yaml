name: Bifrost OracleManager USDC Insufficient Check
time_interval: 30
script: |
  bifrostBN = Bifrost.LatestBlock();
  currentTimestamp = Bifrost.LatestTimestamp();

  feeder = Bifrost.Internal.Feeder.Relayer(bifrostBN);
  decisionInterval = Bifrost.OracleManager.TimeFilter.Decision(bifrostBN, USDC);
  lastDecision = Bifrost.OracleManager.Info.LastTimestamp(bifrostBN, USDC);

  secPastSinceLastDecision = currentTimestamp - lastDecision;
  secPastSinceLastDecision < decisionInterval - 300;
